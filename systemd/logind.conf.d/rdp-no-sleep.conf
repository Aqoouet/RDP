# Installed as /etc/systemd/logind.conf.d/rdp-no-sleep.conf
# Prevents logind idle/lid/power-key actions from suspending the machine.
[Login]
IdleAction=ignore
HandleSuspendKey=ignore
HandleHibernateKey=ignore
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
