# Installed as /etc/systemd/sleep.conf.d/rdp-no-sleep.conf
# Blocks suspend/hibernate so the home PC stays reachable over RDP.
[Sleep]
AllowSuspend=no
AllowHibernation=no
AllowSuspendThenHibernate=no
AllowHybridSleep=no
